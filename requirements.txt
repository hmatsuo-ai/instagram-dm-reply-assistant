# Vercel はこのファイルを直接 pip install する。-r のネストで失敗することがあるため依存をここに列挙する。
# ローカルでも同じファイルを使う（requirements-bot.txt は互換用）。
fastapi>=0.109.0
# Vercel 等の Linux ビルドでは uvicorn[standard]（uvloop 等）が失敗することがあるため通常版
uvicorn>=0.27.0
httpx>=0.26.0
python-dotenv>=1.0.0
pydantic-settings>=2.1.0
openai>=1.12.0
scikit-learn>=1.3.0
numpy>=1.24.0
